--- qt-everywhere-opensource-src-5.3.0-beta/qttools/src/linguist/Qt5LinguistToolsConfig.cmake.in.0001~	2014-03-23 00:12:33.000000000 +0100
+++ qt-everywhere-opensource-src-5.3.0-beta/qttools/src/linguist/Qt5LinguistToolsConfig.cmake.in	2014-04-11 20:21:35.585720585 +0200
@@ -15,7 +15,7 @@ get_filename_component(_realOrig \"$$CMA
 if(_realCurr STREQUAL _realOrig)
     get_filename_component(_qt5_linguisttools_install_prefix \"$$CMAKE_INSTALL_LIBS_DIR/$${CMAKE_RELATIVE_INSTALL_LIBS_DIR}\" ABSOLUTE)
 else()
-    get_filename_component(_qt5_linguisttools_install_prefix \"${CMAKE_CURRENT_LIST_DIR}/$${CMAKE_RELATIVE_INSTALL_DIR}\" ABSOLUTE)
+    get_filename_component(_qt5_linguisttools_install_prefix \"${CMAKE_CURRENT_LIST_DIR}/$${CMAKE_RELATIVE_INSTALL_DIR}../../qt5\" ABSOLUTE)
 endif()
 unset(_realOrig)
 unset(_realCurr)
@@ -32,7 +32,7 @@ if (NOT TARGET Qt5::lrelease)
 
     set_target_properties(Qt5::lrelease PROPERTIES
 !!IF isEmpty(CMAKE_BIN_DIR_IS_ABSOLUTE)
-        IMPORTED_LOCATION \"${_qt5_linguisttools_install_prefix}/$${CMAKE_BIN_DIR}lrelease$$CMAKE_BIN_SUFFIX\"
+        IMPORTED_LOCATION \"${_qt5_linguisttools_install_prefix}/bin/lrelease$$CMAKE_BIN_SUFFIX\"
 !!ELSE
         IMPORTED_LOCATION \"$${CMAKE_BIN_DIR}lrelease$$CMAKE_BIN_SUFFIX\"
 !!ENDIF
@@ -44,7 +44,7 @@ if (NOT TARGET Qt5::lupdate)
 
     set_target_properties(Qt5::lupdate PROPERTIES
 !!IF isEmpty(CMAKE_BIN_DIR_IS_ABSOLUTE)
-        IMPORTED_LOCATION \"${_qt5_linguisttools_install_prefix}/$${CMAKE_BIN_DIR}lupdate$$CMAKE_BIN_SUFFIX\"
+        IMPORTED_LOCATION \"${_qt5_linguisttools_install_prefix}/bin/lupdate$$CMAKE_BIN_SUFFIX\"
 !!ELSE
         IMPORTED_LOCATION \"$${CMAKE_BIN_DIR}lupdate$$CMAKE_BIN_SUFFIX\"
 !!ENDIF
